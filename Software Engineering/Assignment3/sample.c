#include <stdio.h>

int main() {
    int x = 10;
    printf("Initial x = %d\n", x);

    // First change to x
    x = 20;
    printf("After setting x to 20, x = %d\n", x);

    // Second change to x
    x = 30;
    printf("After setting x to 30, x = %d\n", x);

    return 0;
}

